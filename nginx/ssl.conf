server {
    listen 443 ssl;
    server_name api.seu-dominio.com;

    ssl_certificate     /etc/letsencrypt/live/api.seu-dominio.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.seu-dominio.com/privkey.pem;

    location /api/auth/ {
        proxy_pass http://auth_service/auth/;
    }

    location /api/documents/ {
        proxy_pass http://documents_service/documents/;
    }

    location /api/agent/ {
        proxy_pass http://agent_service/agent/;
    }

    location /api/billing/ {
        proxy_pass http://billing_service/billing/;
    }
}
